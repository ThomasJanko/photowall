/**
 * @deprecated Remplacé par la page dédiée /sondage + <PollFab /> (pastille
 * discrète qui y renvoie). Conservé vide pour compat d'imports résiduels.
 */
export function PollModal(): null {
  return null;
}
